import { createSlice } from '@reduxjs/toolkit';

const initialState = {
};

const reducers = {
};

export const slice = createSlice({
  name: 'default',
  initialState,
  reducers
});

export const { reducer } = slice;
