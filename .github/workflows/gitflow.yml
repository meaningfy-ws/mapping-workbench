name: GitFlow Automation

on:
  push:
    branches:
      - develop
      - main
      - feature/**
      - release/**
      - hotfix/**

jobs:
  gitflow-automation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Gitflow Genius Action
        uses: iamfj/gitflow-genius-action@0.1.0-alpha.0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
